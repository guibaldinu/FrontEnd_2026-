# Atividade 6 — Introdução ao JavaScript

- **hello_world.html** — Botão chama `hello()` e exibe "Hello World!!!" via `getElementById`
- **exemplo_aula.html** — Variáveis `const`, `let` e `var`; classe `Carro` com construtor e método `exibirDetalhes()`
- **lampada.html** — Dois botões alteram o estado de uma lâmpada manipulando o DOM
