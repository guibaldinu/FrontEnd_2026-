# Atividade 9 — Componentes com JavaScript

- **Componentes com JavaScript.html** — Funções `criarTitulo()`, `criarParagrafo()` e `criarBotao()` retornam HTML como string. A função `renderizarPagina()` monta tudo e injeta na `div.root` via `innerHTML`. Base conceitual para entender o React.
